export type products = {
    products: {
        image: string
        name: string
        slug: string
        price: string
    }[]

    category: {
        name: string
        type: string
    }
}
